<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.cms.external.mapper.OrganizationMapper">

    <resultMap type="Organization" id="OrganizationResult">
        <result property="id" column="id"/>
        <result property="orgName" column="org_name"/>
        <result property="orgCode" column="org_code"/>
        <result property="parentId" column="parent_id"/>
        <result property="levels" column="levels"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <sql id="selectOrganizationVo">
        select id, org_name, org_code, parent_id, levels, create_time, update_time, remark
        from mid_organization
    </sql>

    <select id="selectOrganizationById" parameterType="String" resultMap="OrganizationResult">
        <include refid="selectOrganizationVo"/>
        where id = #{id}
    </select>

    <select id="selectOrganizationList" parameterType="Organization" resultMap="OrganizationResult">
        <include refid="selectOrganizationVo"/>
        <where>
            <if test="orgName != null and orgName != ''"> and org_name like concat('%', #{orgName}, '%')</if>
            <if test="orgCode != null and orgCode != ''"> and org_code = #{orgCode}</if>
            <if test="parentId != null and parentId != ''"> and parent_id = #{parentId}</if>
            <if test="levels != null"> and levels = #{levels}</if>
        </where>
        order by org_code
    </select>

    <select id="selectOrganizationByCode" parameterType="String" resultMap="OrganizationResult">
        <include refid="selectOrganizationVo"/>
        where org_code = #{orgCode}
    </select>

    <select id="selectChildrenOrganization" parameterType="String" resultMap="OrganizationResult">
        <include refid="selectOrganizationVo"/>
        where parent_id = #{parentId}
        order by org_code
    </select>

    <insert id="insertOrganization" parameterType="Organization">
        insert into mid_organization (
            id, org_name, org_code, parent_id, levels, create_time, update_time, remark
        ) values (
                     #{id}, #{orgName}, #{orgCode}, #{parentId}, #{levels}, #{createTime}, #{updateTime}, #{remark}
                 )
    </insert>

    <update id="updateOrganization" parameterType="Organization">
        update mid_organization
        <set>
            <if test="orgName != null and orgName != ''">org_name = #{orgName},</if>
            <if test="orgCode != null and orgCode != ''">org_code = #{orgCode},</if>
            <if test="parentId != null and parentId != ''">parent_id = #{parentId},</if>
            <if test="levels != null">levels = #{levels},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="remark != null">remark = #{remark},</if>
        </set>
        where id = #{id}
    </update>

    <delete id="deleteOrganizationById" parameterType="String">
        delete from mid_organization where id = #{id}
    </delete>

    <delete id="deleteOrganizationByIds" parameterType="String">
        delete from mid_organization where id in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>