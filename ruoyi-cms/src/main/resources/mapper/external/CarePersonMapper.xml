<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.cms.external.mapper.CarePersonMapper">

    <resultMap type="CarePerson" id="CarePersonResult">
        <result property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="sex" column="sex"/>
        <result property="birthday" column="birthday"/>
        <result property="intoTime" column="into_time"/>
        <result property="outTime" column="out_time"/>
        <result property="mobileNumber" column="mobile_number"/>
        <result property="idNumber" column="id_number"/>
        <result property="orgName" column="org_name"/>
        <result property="orgCode" column="org_code"/>
        <result property="orgId" column="org_id"/>
        <result property="address" column="address"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <sql id="selectCarePersonVo">
        select id, name, sex, birthday, into_time, out_time, mobile_number,
               id_number, org_name, org_code, org_id, address, create_time, update_time, remark
        from mid_care_person
    </sql>

    <select id="selectCarePersonById" parameterType="String" resultMap="CarePersonResult">
        <include refid="selectCarePersonVo"/>
        where id = #{id}
    </select>

    <select id="selectCarePersonList" parameterType="CarePerson" resultMap="CarePersonResult">
        <include refid="selectCarePersonVo"/>
        <where>
            <if test="name != null and name != ''"> and name like concat('%', #{name}, '%')</if>
            <if test="idNumber != null and idNumber != ''"> and id_number = #{idNumber}</if>
            <if test="mobileNumber != null and mobileNumber != ''"> and mobile_number = #{mobileNumber}</if>
            <if test="orgName != null and orgName != ''"> and org_name like concat('%', #{orgName}, '%')</if>
            <if test="orgCode != null and orgCode != ''"> and org_code = #{orgCode}</if>
            <if test="orgId != null and orgId != ''"> and org_id = #{orgId}</if>
        </where>
        order by create_time desc
    </select>

    <select id="selectCarePersonByIdNumber" parameterType="String" resultMap="CarePersonResult">
        <include refid="selectCarePersonVo"/>
        where id_number = #{idNumber}
    </select>

    <select id="selectCarePersonCount" resultType="int">
        select count(*) from mid_care_person
    </select>

    <insert id="insertCarePerson" parameterType="CarePerson">
        insert into mid_care_person (
            id, name, sex, birthday, into_time, out_time, mobile_number,
            id_number, org_name, org_code, org_id, address, create_time, update_time, remark
        ) values (
                     #{id}, #{name}, #{sex}, #{birthday}, #{intoTime}, #{outTime}, #{mobileNumber},
                     #{idNumber}, #{orgName}, #{orgCode}, #{orgId}, #{address}, #{createTime}, #{updateTime}, #{remark}
                 )
    </insert>

    <update id="updateCarePerson" parameterType="CarePerson">
        update mid_care_person
        <set>
            <if test="name != null and name != ''">name = #{name},</if>
            <if test="sex != null and sex != ''">sex = #{sex},</if>
            <if test="birthday != null and birthday != ''">birthday = #{birthday},</if>
            <if test="intoTime != null and intoTime != ''">into_time = #{intoTime},</if>
            <if test="outTime != null and outTime != ''">out_time = #{outTime},</if>
            <if test="mobileNumber != null and mobileNumber != ''">mobile_number = #{mobileNumber},</if>
            <if test="idNumber != null and idNumber != ''">id_number = #{idNumber},</if>
            <if test="orgName != null and orgName != ''">org_name = #{orgName},</if>
            <if test="orgCode != null and orgCode != ''">org_code = #{orgCode},</if>
            <if test="orgId != null and orgId != ''">org_id = #{orgId},</if>
            <if test="address != null and address != ''">address = #{address},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="remark != null">remark = #{remark},</if>
        </set>
        where id = #{id}
    </update>

    <delete id="deleteCarePersonById" parameterType="String">
        delete from mid_care_person where id = #{id}
    </delete>

    <delete id="deleteCarePersonByIds" parameterType="String">
        delete from mid_care_person where id in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <insert id="batchInsertCarePerson" parameterType="java.util.List">
        insert into mid_care_person (
        id, name, sex, birthday, into_time, out_time, mobile_number,
        id_number, org_name, org_code, org_id, address, create_time, update_time, remark
        ) values
        <foreach collection="list" item="item" index="index" separator=",">
            (
            #{item.id}, #{item.name}, #{item.sex}, #{item.birthday}, #{item.intoTime}, #{item.outTime},
            #{item.mobileNumber}, #{item.idNumber}, #{item.orgName}, #{item.orgCode}, #{item.orgId},
            #{item.address}, #{item.createTime}, #{item.updateTime}, #{item.remark}
            )
        </foreach>
    </insert>

    <update id="batchUpdateCarePerson" parameterType="java.util.List">
        <foreach collection="list" item="item" index="index" separator=";">
            update mid_care_person
            <set>
                name = #{item.name},
                sex = #{item.sex},
                birthday = #{item.birthday},
                into_time = #{item.intoTime},
                out_time = #{item.outTime},
                mobile_number = #{item.mobileNumber},
                id_number = #{item.idNumber},
                org_name = #{item.orgName},
                org_code = #{item.orgCode},
                org_id = #{item.orgId},
                address = #{item.address},
                update_time = #{item.updateTime},
                remark = #{item.remark}
            </set>
            where id = #{item.id}
        </foreach>
    </update>

</mapper>